let texto =parseInt(prompt("ingrese texto"))
let = texto.includes = ("ADSO") && texto("desarrolladores")
 switch (true) {
   case 1:
     console.log("contiene las palabras");
     break;

   default:
     console.log("no contiene las palabras");
     break;
 }
